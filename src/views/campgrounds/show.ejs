<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Show Campground</title>
  </head>
  <body>
    <h1><%= campground.title %></h1>
    <h2><%= campground.location %></h2>
    <header><a href="/campgrounds/<%= campground._id %>/edit">Edit</a></header>

    <form
      action="/campgrounds/<%= campground._id %>?method=DELETE"
      method="POST"
    >
      <button>Delete</button>
    </form>

    <footer>
      <a href="/campgrounds">Back</a>
    </footer>
  </body>
</html>
